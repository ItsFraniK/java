var counter = 5; // присвоение значения переменной
	while (counter < 10) { // условие 
		counter++;
		document.write("counter" +  counter);
		break;
		document.write("Эта строка не выполнится");
	}